flowchart
the following steps are in order


input file processing
  get input file from user
  ->
  spit out molecule, atomic orbital basis, and number of occupied orbitals
  

calculate 1e and 2e integrals


calculate h core
  from 1e integerals


diagonalise h core
calculate density matrix


SCF loop
  calculate fock matrix
  diagonalise fock matrix
  calculate density matrix

  check convergence


if converged
  return HF energy
  return hessian and gradients in order to determine (non) stationary point
if not converged
  ...
